node_modules/
.next/
out/
build/
.env
.env.local
.env*.local
*.log
venv
.DS_Store
.vercel/
__pycache__/
*.db
*.db-*
/media
# Auto-regenerated by @payloadcms/next's build tooling at this conventional
# path; unused — the real importMap.js the admin route imports lives at
# src/app/(payload)/importMap.js and IS tracked.
src/app/(payload)/admin/importMap.js

# TypeScript incremental build cache — not source, don't track.
tsconfig.tsbuildinfo

# AI coding agent config — not tracked in this repo.
AGENTS.md
CLAUDE.md
